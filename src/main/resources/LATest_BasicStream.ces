
Stream temperature;
int max = 0;

function getLatestMaximum(Stream data) {
    if (data > max) {
        return max;
    } else {
        max = data;
        return max;
    }
}

print(getLatestMaximum(temperature));